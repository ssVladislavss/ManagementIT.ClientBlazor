

@*<MudDialog>
    <DialogContent>
        <MudForm @bind-IsValid="@success">

            <MudSelect @bind-Value="Model.Position"
                       Required="true"
                       Label="Отделение"
                       RequiredError="Поле нельзя оставить пустым">
                @foreach (var position in Model.SelectedPosition)
                {
                    <MudSelectItem Value="@position">
                        @position.Name
                    </MudSelectItem>
                }
            </MudSelect>

        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Info" Disabled="@(!success)" OnClick="Confirm">Подтвердить</MudButton>
        <MudButton OnClick="Cancel">Отмена</MudButton>
    </DialogActions>
</MudDialog>


@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public CreateOrEditEmployeeViewModel Model { get; set; }

    private bool success;

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private void Confirm()
    {
        MudDialog.Close(DialogResult.Ok(Model));
    }
}*@
